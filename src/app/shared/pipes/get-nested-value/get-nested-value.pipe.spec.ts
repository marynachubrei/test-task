import { GetNestedValuePipe } from './get-nested-value.pipe';

describe('GetNestedValuePipe', () => {
  it('create an instance', () => {
    const pipe = new GetNestedValuePipe();
    expect(pipe).toBeTruthy();
  });
});
