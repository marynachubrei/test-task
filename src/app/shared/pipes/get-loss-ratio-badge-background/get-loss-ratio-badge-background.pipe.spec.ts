import { GetLossRatioBadgeBackgroundPipe } from './get-loss-ratio-badge-background.pipe';

describe('GetLossRatioBadgeBackgroundPipe', () => {
  it('create an instance', () => {
    const pipe = new GetLossRatioBadgeBackgroundPipe();
    expect(pipe).toBeTruthy();
  });
});
